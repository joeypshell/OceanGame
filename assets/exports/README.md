# Asset Exports

Store committed runtime-ready art exports here when a Godot scene or resource actually uses them.

Do not put platform builds, Godot import caches, `.import` files, or local review screenshots here.

## Subfolders

- `sprites/`: runtime sprite images.
- `ui/`: runtime UI images/icons.

