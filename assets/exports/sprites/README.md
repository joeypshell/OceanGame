# Sprite Exports

Runtime sprite files live here after a source asset is selected and exported.

Only commit files that are used by the Godot project or intentionally staged for the next implementation issue.

