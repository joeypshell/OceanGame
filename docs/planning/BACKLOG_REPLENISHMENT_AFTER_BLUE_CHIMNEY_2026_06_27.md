# Backlog Replenishment - After Blue Chimney Pocket - 2026-06-27

Status: next ordered issue batch.

## Purpose

Blue Chimney Pocket is now a visible, named, staged lower pocket with broad return orientation, a condition signal, visual-only reverse draft timing, reset/bounds guardrails, and Playwright evidence.

The next work should make the pocket worth entering during ordinary play without expanding into a full cave network, minimap, combat system, or broad economy. Keep the focus on player-visible route decisions and cheap verification.

## Current Implemented Baseline

- `Resonance Key I` exists and opens the first hatch-specific route promise.
- `Resonance Alcove` exists with one run-scoped research note and extraction memory.
- `Shelf Drop Connector` connects East Shelf to lower route space.
- `Blue Chimney Pocket` exists below Shelf Drop with a named landmark, `Reverse Draft` return cue, `Rare Signal` opportunity, visual timing pulse, no-minimap guardrails, oxygen-margin review, and deterministic Playwright capture.

## Direction

The next batch should prove that a connected lower pocket can become a small gameplay destination:

1. Add one narrow Blue Chimney research payoff.
2. Preserve return pressure and no-minimap orientation.
3. Add one optional existing-material reason to consider the route if evidence supports it.
4. Capture the payoff state deterministically.
5. Plan the next branch only after the pocket has a reason to exist.

## Issue Batch

1. Plan the `Blue Chimney` research payoff contract.
2. Add `Blue Chimney` proximity interaction and prompt.
3. Add extraction result memory for `Blue Chimney` research.
4. Add reset/save guard coverage for `Blue Chimney` research state.
5. Add one optional existing-resource candidate near `Blue Chimney`.
6. Add lower-pocket route goal copy after `Blue Chimney` research exists.
7. Add Playwright capture for `Blue Chimney` payoff state.
8. Review `Blue Chimney` readability and oxygen after payoff.
9. Plan the next route fork after `Blue Chimney`.
10. Refresh roadmap/backlog after the `Blue Chimney` payoff batch.

## Guardrails

- Keep `Blue Chimney` knowledge-first. The first payoff should be a run-scoped note, not a durable quest chain.
- Do not add a minimap, route graph, exact locator, field-guide objective, quest checklist, broad route journal, or objective arrow.
- Do not add combat, capture, harvesting, weapons, monster loot, or a broad creature collection loop.
- Do not add full procedural generation, full cave networks, region select, base management, or new resource tiers.
- Existing-resource candidates are allowed only if they preserve depth-band readability and do not create extra mandatory pickup pressure.
- Keep oxygen tuning unchanged unless evidence from the review issue shows a local route/readability fix is insufficient.
- Continue using focused logic tests for state ownership and Playwright captures only where route placement or visual evidence changed.

## Verification Pattern

- Use `docs` for planning-only issues.
- Use `quick` for GDScript behavior, state ownership, save/load, reset, and goal/copy changes.
- Use `visual` only for the issue that adds or changes Playwright route evidence.
- Always run `git diff --check` before close.
