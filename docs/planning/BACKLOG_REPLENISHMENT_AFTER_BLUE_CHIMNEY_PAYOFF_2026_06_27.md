# Backlog Replenishment - After Blue Chimney Payoff - 2026-06-27

Status: next ordered issue batch.

## Purpose

The `Blue Chimney` lower pocket now has a reason to exist: a run-scoped draft-reading interaction, extraction memory, optional existing-resource route value, reset/save guard coverage, and deterministic payoff capture.

The next work should turn the lower route from a mostly linear chain into the first readable fork. The selected fork is `Silt Vein Fork`, documented in `docs/planning/NEXT_ROUTE_FORK_AFTER_BLUE_CHIMNEY_2026_06_27.md`.

## Current Implemented Baseline

- `East Shelf Spur` is the first right-side route.
- `Shelf Drop Connector` extends that route downward-right.
- `Resonance Key I` and `Resonance Alcove` prove the first hatch-specific access promise.
- `Blue Chimney Pocket` is a compact lower pocket with landmark, return cue, signal opportunity, optional existing-resource candidate, run-scoped research payoff, reset/save guards, and Playwright evidence.
- `BLUE_CHIMNEY_READABILITY_OXYGEN_REVIEW_2026_06_27.md` keeps oxygen tuning unchanged and prefers local readability fixes if confusion appears.

## Direction

The next batch should prove a small lower-route choice:

1. Scaffold `Silt Vein Fork` below or beyond `Blue Chimney`.
2. Add one readable safe branch, `Lantern Silt Nook`.
3. Add one sealed/deeper promise branch, `Blackwater Crack`.
4. Add one compact run-scoped payoff to the safe branch.
5. Capture the fork state deterministically and review readability before moving wider.

This remains map-growth work, not a broad systems push.

## Issue Batch

1. Scaffold `Silt Vein Fork` geometry and bounds.
2. Add `Silt Vein Fork` landmark metadata and return orientation.
3. Add `Lantern Silt Nook` safe branch scaffold.
4. Add `Blackwater Crack` sealed deeper promise.
5. Plan `Lantern Silt Sample` payoff contract.
6. Add `Lantern Silt Sample` interaction and prompt.
7. Add extraction result memory for `Lantern Silt Sample`.
8. Add reset/save guard coverage for `Silt Vein Fork` run state.
9. Add deterministic Playwright capture for the staged fork.
10. Review `Silt Vein Fork` readability and oxygen after first payoff.

## Guardrails

- Keep the fork small, authored, and player-visible.
- Do not add a minimap, route graph, exact locator, quest checklist, field-guide objective, broad journal, or objective arrow.
- Do not add a full cave network, procedural generation, region select, new biome family, combat, capture, harvesting, weapons, monster loot, or base-management layer.
- Do not add new oxygen rules, decompression, ascent penalties, pressure damage, oxygen refills, or route tolls.
- Do not add new durable save fields unless a later issue explicitly promotes a route memory into progression.
- Use run-scoped state for the first payoff and reset it between expeditions.
- Preserve existing pressure gates, predators, scans, cargo, resources, upgrades, extraction, failure, Wreck Echo, East Shelf, Shelf Drop, Resonance Alcove, and Blue Chimney behavior.
- Keep verification cheap: logic tests for behavior/state, docs tier for planning, visual tier only for route placement/camera evidence.

## Verification Pattern

- Use `docs` for planning-only issues.
- Use `quick` for GDScript behavior, state ownership, save/load, reset, and copy changes.
- Use `visual` only for the issue that adds or changes Playwright route evidence.
- Always run `git diff --check` before close.
