# Silt Vein Fork Readability And Oxygen Review - 2026-06-27

Status: pass with local-readability watchlist; keep oxygen tuning unchanged.

## Context

This review follows the first `Silt Vein Fork` implementation batch:

- small lower-route fork below `Blue Chimney`,
- named `Silt Vein Fork` landmark metadata,
- safe left branch scaffold, `Lantern Silt Nook`,
- sealed right branch promise, `Blackwater Crack`,
- one run-scoped `Lantern Silt Sample` interaction,
- compact extraction memory for the sample,
- reset/save guard coverage for the run-scoped sample,
- deterministic Playwright capture for the staged fork.

The review uses the current staged capture evidence from `silt-vein-fork-staged.png` plus the route contracts in `NEXT_ROUTE_FORK_AFTER_BLUE_CHIMNEY_2026_06_27.md` and `LANTERN_SILT_SAMPLE_PAYOFF_CONTRACT_2026_06_27.md`.

## Judgment

Do not change global oxygen numbers yet.

The fork is doing the right structural job: it creates the first small lower-route choice after `Blue Chimney`, gives the safe branch one compact knowledge payoff, and keeps the deeper branch as a visible promise instead of a new system. The route adds no direct oxygen cost, cargo requirement, hidden pressure penalty, forced movement, predator mutation, durable quest state, or mandatory objective.

The staged capture shows the current oxygen/readability risk is local presentation, not survival math. The HUD remains readable, the base direction points broadly `up-left`, and the fork has enough negative space to avoid the worst visual pileup. However, the fork still reads as placeholder geometry more than a strong authored choice. The safe-left and sealed-right branch meanings should become clearer before the route grows into a larger cave or region.

## Readability Review

Current pass:

- The route now contains a visible lower choice instead of another single linear pocket.
- The active HUD does not obscure the fork decision area in the staged capture.
- Base direction remains useful from the staged fork view.
- The lower-route space is not yet overloaded by rewards, scans, predators, or objective text.
- `Lantern Silt Sample` gives the safe branch a reason to exist without adding cargo or durable progression.
- `Blackwater Crack` remains a deferred promise and should not be treated as the next objective yet.

Watchlist:

- Strengthen the local shape language so the player reads `left branch = safer nook` and `right branch = sealed/deeper crack` faster.
- Keep the silt ribs, lantern glow, and blackwater seal visually separated; do not stack all cues in one bright cluster.
- Make the return-current cue back toward the fork/Blue Chimney stronger if players hesitate after taking the sample.
- If the sample prompt is missed, first adjust local placement or prompt proximity before adding a quest objective or minimap marker.
- If the sealed branch looks like a collision bug, add a clearer closed-mouth silhouette or seal cue rather than opening the route.

## Oxygen Review

Keep current constants:

- passive drain: `0.95` oxygen per second,
- pickup cost: `1` oxygen,
- first-time scan cost: `2` oxygen,
- burst cost: `4` oxygen,
- predator contact cost: `5` oxygen,
- base max oxygen: `30`,
- `Oxygen Tank I` max oxygen: `40`.

Reopen oxygen tuning only with evidence that:

- `Oxygen Tank I` cannot support a clean Blue Chimney -> Silt Vein Fork -> Lantern Silt Nook -> return route without extra actions or predator mistakes,
- low/critical warnings arrive too late for a player already at the fork to choose extraction,
- manual play shows players consistently overcommit because `Blackwater Crack` reads as mandatory,
- local route-cue fixes fail to make return orientation understandable.

## Recommendation

Treat this as a successful first fork, not a rebalance trigger.

The next route work may move forward, but it should stay small and evidence-driven:

1. Improve local fork silhouettes and branch contrast before adding more lower-route nodes.
2. Keep `Blackwater Crack` sealed until a later issue explicitly scopes access.
3. Add no minimap, exact locator, checklist, route graph, combat, harvesting, procedural cave system, oxygen toll, or new upgrade tier as part of this review.
4. Use targeted Playwright captures only when route placement or visual readability changes.

## Verification Note

This review changes no scripts, scenes, resources, oxygen constants, costs, movement, upgrades, save/load behavior, route collision, or Playwright tests. It records the post-payoff readability and oxygen decision so the next planning batch can move forward without reopening broad early-slice tuning.
