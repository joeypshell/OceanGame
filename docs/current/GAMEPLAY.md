# Current Gameplay

This file describes implemented behavior. If gameplay, controls, runtime, deployment, or workflow changes, update this file before closing the related issue.

Near-term work is tracked in `docs/current/ROADMAP.md` and GitHub Issues.

Current planning posture: the active milestone is `First Real 2D Environment Slice`. The project is correcting the first playable room before broader route/content expansion: keep Emergency Week pressure visible, but make the starting side-view ocean read as a real space with open water, solid ledges/walls, object pockets, compact HUD framing, and reusable visual layers.

Current vertical-slice status: the prototype is structurally playable, but visual readability has regressed as the route grew. Treat current Area 01 visual pileup as a known closeout blocker until the player can distinguish background, terrain, return cues, hazards/timing cues, pickups, knowledge payoffs, and locked-route promises at normal play scale. The current Area 01 readability pass separates cue families by intent: resource pickups keep the clearest cargo silhouettes, knowledge payoffs use compact cyan or amber cores with subdued halos, locked/future route promises use darker mouths plus quiet seal/glint treatment, and return/timing support stays lower-opacity than recoverable objects.

## Product Behavior

- The project launches into a side-view vertical ocean slice with a placeholder controllable submersible.
- The selected direction is a 2D underwater roguelite where each dive is an expedition into alien ocean biomes.
- Intended first loop: dive from a safe base, gather resources, scan lifeforms, avoid or study dangerous creatures, return before oxygen runs out, and craft upgrades that allow deeper future dives.
- Current world model: persistent geography with seeded expedition variation. The player should learn stable places, depth bands, safe return orientation, major gates, and upgrade-locked route promises while each expedition can vary resource placement, route/reward cluster pattern, and predator route selection from authored candidates.
- Full procedural biome generation, complete ocean rerolls, and real-calendar daily challenge rules are not implemented in the current prototype.
- Current controls: move with WASD or arrow keys, and press Spacebar for `Burst Thruster` during active dives.
- Extraction controls: return to the safe base and press E or Enter to end the dive successfully.
- Next expedition: press R after extraction or oxygen failure to prepare the next seeded expedition; the result panel frames this as the ocean shifting again rather than a prototype restart.
- Survival loop: Chapter 1 is an `Emergency Week` prototype. Food, Water, and Power start at 3. The ready panel names these as `Base needs`, shows the nightly cost, and explains that the shallow supply cache fills the lowest need while using cargo space. Each night consumes 1 of each need after the expedition resolves. A need at 0 applies a staged oxygen penalty to the next expedition; the night report names the max-oxygen penalty, and a need below 0 collapses the week. `Water Filter I` is the first starter-material surface upgrade: it spends `Driftwood x1` and `Quartz Glass x1` for an immediate `+1 Water` reserve, giving shallow material pockets a direct survival purpose. Pressing R after collapse restarts the Emergency Week while keeping major prototype progression such as upgrades, banked resources, discoveries, and best depth.
- Survival supplies: a shallow `Supply Cache` can be recovered once per expedition. It adds the currently most-needed survival supply to the normal cargo hold, so it competes with resource cargo. Extracting banks survival supplies into Food/Water/Power instead of the upgrade resource bank. Oxygen failure still loses carried supplies.
- Input is routed through Godot actions: `move_left`, `move_right`, `move_up`, `move_down`, `interact`, `restart_dive`, `scan`, and `burst_thruster`. F3 toggles development telemetry visibility. When development telemetry is visible, F4 cycles the current expedition condition for visual review, F5 advances the current review seed, and F6 stages the Wreck Echo route/result visual smoke states; these debug helpers are ignored while telemetry is hidden. F7 and F8 stage low and critical oxygen respectively for automated visual smoke capture during an active dive. Exported/web debug commands can also stage `mirror_kelp_pass`, `mirror_kelp_tideglass`, and `mirror_kelp_mirrorfin` without granting upgrades or writing durable progression.
- Future platform/input targets are planning-only for now: eventual mobile/iPhone support should be landscape locked with touchscreen controls, and later controller support should map onto the same semantic Godot actions rather than changing gameplay rules. See `docs/planning/FUTURE_PLATFORM_INPUT_TARGETS_2026_06_26.md`.
- Current movement uses acceleration, drag, and bounded side-view world limits so the submersible cannot leave the prototype space. The bounds now preserve the existing vertical base column while allowing a right-side branch and first lower connector for the current expanded-region route sequence.
- The first scene starts the player at a visible surface boat/shallow lab base near the top of the water column.
- The camera follows the player through the dive with limits that keep the side-view space readable across the current base column, the right-side route chain, and the first `Outer Shelf` footprint without implying a full lower biome yet.
- A world-space `RouteChoiceBand` remains in the scene as review scaffolding, but it is hidden by default during normal play while the first real Area 01 environment slice is being established. The HUD and terrain/object placement should now carry broad dive guidance instead of visible `SAFE BANK` / `RESEARCH ROUTE` route-label bands.
- `East Shelf Spur` has a first-pass authored side-route scaffold to the right of the current column. It now starts with a stronger mid-depth branch mouth: a dark route shadow, cyan rim frame, visible route opening, broader approach current, and heavier shelf silhouettes so normal play reads the side route as a place the map opens sideways. A soft pulsing `CurrentSurgeLane` near the route gap is a non-combat timing cue only: it suggests waiting or choosing a line without pushing, damaging, blocking, or changing oxygen. `East Shelf Arch` is the first stable side-route landmark and has soft return-current ribs pointing left/up toward the base column. `Shelf Glimmer` is the first seeded side-route opportunity: under the deterministic `Rare Signal` condition, a subtle glimmer appears near the arch as a visual research ping, not a collectible, guaranteed reward, map marker, or checklist objective. The far end now has a cave-like `East Shelf Pocket` destination with a darker back wall, stronger rim, foreground shelf lip, two landmark spires, interior glow pool, return-current cue, proximity prompt, and bright `SignalCore` payoff so it reads as an actual place rather than only a translucent route hint; interacting recovers the run-scoped signal core, visibly dims the core, repeat interactions report that it was already recovered, and successful extraction acknowledges the core as compact result memory that points toward the sealed route. `Sealed Shelf Hatch` is the first visible upgrade-locked promise in this branch: it starts as `ECHO LOCK` and changes to `ECHO PING` when `Echo Lens I` is owned. Just beyond it, `DeepRoutePromise` adds a dark sealed lower route mouth, pressure wash, seal lip, glint, and small `DEEP ROUTE SEALED` label so the pocket clearly points toward future depth without becoming a current objective. If the player returns with East Shelf or Drop Echo research while `Echo Lens I` is owned, result copy can say the lab can plan a future `Resonance Key`; this still does not add an interior, quest, reward, map marker, checklist, upgrade entry, or new traversal system. `Shelf Drop Connector` is a short authored downward-right continuation below the East Shelf pocket area. It currently adds a visible connector mouth, subdued drop shelves, a faint down-current thread, `Drop Arch` as the first lower-connector landmark, an up-left return-current cue, a turnback hint, and enough camera/world space to stage the route. `Drop Echo` is a small inspectable lower-connector research opportunity near the turnback point: interacting records one run-scoped note and successful extraction can acknowledge it as compact result memory, without adding cargo, a scan discovery, durable quest state, minimap, field guide, combat, or harvesting. `Blue Chimney Pocket` is now scaffolded just below the Shelf Drop turnback as a compact lower-pocket shell with a mouth, rim, floor, blue chimney landmark shapes, a visible `SurveyCore` payoff, a softly pulsing `Reverse Draft` return cue pointing up-left toward `Drop Arch`, and a closed lower crack. The reverse draft is a visual timing/navigation cue only; it does not push, damage, drain oxygen, move cargo, mutate predator/pressure/progression state, or change route access. The nearby interaction recovers one run-scoped `Blue Chimney` survey core for the current expedition; repeat interaction reports that the core was already recovered, direct oxygen/cargo/progression state is unchanged, the core visibly dims after recovery, and successful extraction can acknowledge that the core points toward a deeper side-route below Shelf Drop. Under `Rare Signal`, a subtle Blue Chimney signal wash can appear there and the completed-upgrade ready goal can suggest checking East Shelf or Blue Chimney if oxygen allows, then returning safely. `Silt Vein Fork` is now scaffolded just below the Blue Chimney lower crack as the first small lower-route split, with a subtle fork mouth, pale silt ribs, a left branch hint, a darker right branch hint, and a lower turnback lip. `Lantern Silt Nook` is a compact safe left-branch scaffold with a small mouth, faint lantern glow, silt shelf, local return-current cue back toward the fork, and one run-scoped `Lantern Silt Sample` interaction; repeat interaction reports that the sample was already stored, direct oxygen/cargo/progression state is unchanged, and successful extraction can acknowledge that the sample confirms the left branch is the safer Silt Vein route. `Blackwater Crack` is a sealed right-branch deeper promise with a dark mouth, pressure-dark wash, quiet seal lip, closed shard, gate badge, gate label, proximity readback, and one short hidden `Blackwater Sill` route segment. It starts as `RESONANCE SEAL`, changes to `KEY NEEDED` once `Echo Lens I` is owned, and changes to `KEY READY` once `Resonance Key I` is owned. At `KEY READY`, the short `Blackwater Sill` appears beyond the crack with a dark mouth, local floor silhouette, broad return-current cue back up-left toward Silt Vein / Blue Chimney, a small turnback seal for future growth, a subtle violet `PressureShutter` pulse with two pressure ribs, and one visible `Blackwater Trace` knowledge payoff. The Blackwater pressure pulse is a readability/timing cue only; it does not push, damage, drain oxygen, move cargo, mutate predator/pressure/progression state, bypass locks, or change route access. Interacting in the open sill records the run-scoped trace, visibly dims the trace core, repeat interactions report that it was already recorded, direct oxygen/cargo/progression state is unchanged, and successful extraction can acknowledge that the trace confirms the right branch carries a deeper route signal. The current Blackwater pass does not add cargo, durable Blackwater state, damage, block with hidden pressure rules, combat, harvesting, minimap/checklist behavior, a new upgrade family, or a full cave system. The current Silt Vein review keeps oxygen tuning unchanged and treats branch silhouette, return-cue strength, and sealed-branch readability as local watchlist items before larger cave or region growth. These fork/nook/crack/sill nodes add no resource, scan, interior, minimap, quest state, combat, harvesting, procedural cave behavior, or new progression state.
- Blackwater return orientation now adds a small scene-native return-chain cue inside `Blackwater Sill`; ready/status/result/base-direction copy can compactly name the Silt Vein / Blue Chimney / Drop Arch chain without adding minimap, exact locator, route graph, field-guide, checklist, objective tracker, interaction, reward, resource, or durable state.
- `Outer Shelf` now contains a wider visual route plate with darker upper/lower shelf rims, a mid-shelf back plate, Glass Rim step structure, a survey perch, and a lower foreground shelf so the first Area 02 route reads as a larger place at normal play scale before adding more mechanics. It also contains one run-scoped `Outer Shelf survey` knowledge payoff near the `Glass Rim Cut` timing branch and an existing `Kelp Fiber` deep-reward spawn candidate nearby. The choice is intentionally narrow: recover the survey, spend time/cargo on nearby material if that run places it there, do both, or return through Mirror Kelp / Wide Reef / Hollow Reef. After the survey, the Glass Rim slackwater cue now reads as a local wait/cross/turn-back decision under oxygen pressure while staying non-durable: no quest checklist, exact map marker, hidden route state, cargo mutation, or global oxygen tuning. The passive `Glass Ray Drifter` now loops across that cue as a scannable timing teacher; scanning it can produce current-run Outer Shelf memory and result copy about reading slackwater without adding combat, harvesting, loot, capture, field-guide objectives, exact locators, or new creature inventory. A small `RimGlassReading` payoff now sits beyond the timing branch and before the sealed future promise; recovering it creates a run-scoped Glass Rim result memory that competes with nearby existing Kelp Fiber cargo and the safe return route, while adding no cargo, durable route state, map/checklist UI, resource family, loot table, combat, or upgrade purchase. The survey visibly dims after interaction, repeats as already recorded, does not add cargo or durable save state, and successful extraction can remember Outer Shelf as the newest route place. Surface result and recent-expedition copy can now tease one broad next expedition hypothesis for that memory: try the Glass Rim timing branch or bank the Outer Shelf cargo if oxygen allows, without adding a quest checklist, exact map marker, or durable route state. Past the visible `Glass Rim Cut`, a quiet `RIM SEAL NEEDED` promise marks a future return target tied to that remembered landmark. It has subdued mouth, veil, bars, and glint visuals only; it adds no interaction, collision, resource, upgrade entry, objective tracker, save state, or route access.
- The first local Blackwater readability tune keeps oxygen unchanged because the evidence showed framing and cue-crowding issues rather than a survival-margin failure. The active open-gate HUD line is shorter, the return-current, rare-signal, pressure, and trace cues occupy clearer local bands, and the authored ocean/deep-haze backdrops now cover the current Blackwater camera bounds.
- `Dusk Trench` is now scaffolded as the first compact route continuation beyond `Blackwater Sill`. It inherits the existing Blackwater gate through scene hierarchy, extends the current camera/world bounds only enough for one darker right/down route segment, and adds a trench mouth, open-water negative space, lower shelf silhouette, far turnback shadow, and up-left return-current cues back toward Blackwater / Silt Vein / Blue Chimney. It now has passive `LandmarkMetadata`, broad base-direction copy that routes back through Blackwater / Silt / Blue, and run-scoped reach memory for the result place line when the player physically reaches the trench. Supported Dusk reach evidence also owns the deepest route-choice line and recent-log `route Dusk Trench` memory, taking priority over Blackwater / Blue / Silt evidence without adding durable route state. A local `LowVisibilityCue` now comes from reusable `DuskLowVisibilityCue.tscn` and adds dark blue-violet murk and silt ribs as a presentation-only pressure/timing read; it is visually separate from green return currents and does not add oxygen drain, cargo mutation, collision, knockback, predator state, pressure locks, or progression changes. `Glass Kelp Ledge` is the first calm side pocket off the trench, with glassy kelp silhouettes, a safer ledge shelf, calm-pocket water, and a return-current cue back toward the trench. Its single `Glass Kelp reading` interaction visibly dims after recovery, repeats as already recorded, and successful extraction can remember that the ledge is a safer Dusk Trench side route. `HollowReefCave` is now the first visible cave-like side branch farther along Dusk Trench, with a documented source-art sprite slot pointing to `assets/exports/sprites/landmark_hollow_reef_cave_sprite_v1.svg`, a dark mouth, cool reef rim, threshold water, local reef teeth, a distinct entrance lip/ribs, an exit return ribbon/rib, a pale return-chain with ribs/fleck back toward Dusk/Blackwater, a back-wall/end read, a short authored `InteriorLane` beyond the first reading payoff, a visually distinct optional `UpperShelfChoice` above the main lane, a small `ShelteredResourcePocket` that can host existing `Glow Plankton` through the authored deep-reward spawn pool, a low-alpha lavender `TimingCurrentCue` inside the lane that pulses as a wait/pass navigation read, a lane return-current cue back toward the mouth, one visible `WideReefChamber` beyond the lane with open side-to-side water, upper/lower shelf framing, a cool far-wall silhouette, a subtle traversal-clear band, a low foreground shelf edge, passive `Glass Rib Span` and `Low Crown Shelf` landmarks, a far/mid/entry return-current chain pointing back toward Hollow Reef, a quiet future-choice shadow, one far `FarTurnbackCue` labeled `REEF CONTINUES LATER / RETURN VIA HOLLOW`, one `WreckSalvagePocketEntrance` labeled `SALVAGE CLOSED` on the far side of the chamber, a warm visual-only `CUTTER NEEDED` port, a visible `Salvage data cache` inside that entrance, an optional existing `Shell Fragments` deep-reward cargo candidate below-left of the cache, one visible `Hollow Reef cave reading` payoff, and `MirrorKelpPass` as a second Area 01 branch scaffold off the chamber with a dark mouth, reflective backwater, cool mirror-kelp curtains, floor/ceiling framing, entry plus loop return-current ribbons/ribs, a quiet chamber-loop silhouette, broad `RETURN VIA WIDE REEF` copy, passive place metadata, and a stable prototype label. Mirror Kelp Pass also contains one run-scoped `Tideglass Sample` knowledge payoff that visibly dims after recovery, repeats as already recorded, and adds compact extraction research about a Mirror Kelp return-current reading without adding cargo, save state, resource families, or route checklist behavior. It can also host existing `Kelp Fiber` as an optional deep-reward cargo candidate below the Tideglass payoff, creating a local cargo-vs-knowledge choice without adding an extra pickup node, inventory slot, stack rule, loot table, or crafting recipe. A passive scannable `Mirrorfin Drift` now moves across a subtle reflection lane in the pass, teaching the player to read the shimmer break and safe return timing without adding collision, damage, harvesting, combat, drops, capture, field-guide objectives, or checklist behavior. Under `Kelp Bloom`, the Mirror Kelp approach gains a visible wash/rib, stronger reflective backwater, thicker curtain alpha, and compact ready/briefing copy so the route feels denser for that expedition without changing geography, access, oxygen, cargo, progression, save behavior, Tideglass interaction, or Mirrorfin non-combat rules. Beyond the current payoff, `DeepKelpSealPromise` adds a closed deep-kelp route mouth, subtle cool pressure veil, kelp seal bars, future glint, and `DEEP KELP SEALED` copy so the branch points toward a remembered future return without adding an upgrade, interaction, collision, pickup, save state, oxygen/cargo mutation, map marker, checklist, combat, harvesting, or interior. Interacting with the salvage cache records one run-scoped data note, visibly dims the cache, repeats as already recovered, and successful extraction promotes that evidence into the normal `Salvage Data Cache` discovery prerequisite for `Salvage Cutter I`; taking the shell cargo remains the normal resource-pickup loop, so the player can spend oxygen/time on cargo, knowledge, both, or the safe return route. When recent-session route memory is `Wide Reef Chamber` and the existing upgrade queue is otherwise complete, the ready goal can point back to Wide Reef with `Salvage Cutter I` while keeping the return route broad. The existing `Calm Current` expedition condition subtly strengthens the chamber return-current chain while preserving stable geography, access, oxygen, cargo, progression, and save behavior. The branch also has a closed `DeeperReefPromise` mouth labeled `DEEP REEF CLOSED` beyond the current payoff. Interacting records the Hollow Reef reading once per run, visibly dims the reading core, repeats as already recorded, and successful extraction can remember that Hollow Reef is a side-cave branch off Dusk Trench; a first-time `Hollow Reef Skitter` scan can also support compact Hollow Reef place/route memory. The current Hollow Reef / Wide Reef / Mirror Kelp pass does not add a full cave system, new resource type, hidden oxygen tax, cargo-capacity change, predator mutation, pressure boundary, route collision, knockback, minimap/checklist behavior, procedural generation, new broad upgrade family, durable quest state, durable route state, salvage loot table, salvage inventory, salvage resource, crafting tree, combat, harvesting, cutter interaction, or wreck interior.
- `Salvage Cutter I` is now a normal Upgrade Bay entry. It is locked until the player recovers the `Salvage Data Cache` from the Wide Reef Chamber and extracts safely; after that, it costs existing resources only (`Kelp Fiber x1`, `Shell Fragments x2`) and its owned state is scoped to opening the sealed Wide Reef salvage pocket. This replaces the earlier planned/locked cutter promise copy and still does not add salvage inventory, a salvage resource, a broad crafting tree, combat, harvesting, a minimap, a quest log, or multiple cutter tiers.
- When `Salvage Cutter I` is owned, the existing Wide Reef salvage pocket changes from `SALVAGE CLOSED` / `CUTTER NEEDED` to `SALVAGE OPEN` / `CUTTER READY`, hides the lock bars, fades the cut hatch panel, and reveals a small `OpenedPocketLane` shell with safe-return copy back through Hollow Reef. Without the cutter, the pocket remains sealed and the opened lane stays hidden. This is presentation and route-readability state driven only by owned upgrade state; it does not spend oxygen, move cargo, add a wreck interior, add route save state, or create salvage inventory.
- Inside the opened Wide Reef salvage pocket, `Salvage Manifest` is the first cutter-only payoff. Interacting records one run-scoped knowledge note, visibly dims the manifest, repeats as already recovered, and successful extraction can remember `Salvage Pocket` in result/recent-expedition copy. The nearby existing `Shell Fragments` candidate remains the cargo pressure, so the local decision is knowledge, cargo, both, or safe return. This still does not add salvage inventory, a salvage resource, loot tables, crafting, durable route state, quest/checklist behavior, combat, harvesting, or a wreck interior.
- Tideglass recovery or Mirrorfin observation can make `Mirror Kelp Pass` the compact route memory for result copy, Recent Expeditions, and the next prepared ready goal. This memory is session-only, points broadly back through Wide Reef/Hollow Reef, and does not create map markers, checklist objectives, field-guide tasks, exact locators, or durable route state.
- `LanternRayRoute` is the first non-combat lower-route creature presence near Dusk Trench. It is a passive scannable behavior observation: scanning records `Lantern Ray` as a normal durable discovery, repeat scans are free and compact, and result copy frames the finding as lower-route movement observed without fighting. A pale timing/avoidance lane moves with the ray as a readable wait/pass/route-around cue, distinct from green return currents and dark pressure cues. Repeat expeditions place the ray on one deterministic authored patrol for the current seed, route cluster, and condition; `Low Visibility` can prefer a higher safer glide. The selected route is runtime/debug telemetry only and is not saved as durable progress. Extracting after the scan can remember `Lantern Ray Route` as the broad place learned that day, and the recent expedition log can show `route Lantern Ray` when no deeper route evidence takes priority; Dusk, Blackwater, Blue Chimney, Silt Vein, Shelf Drop, East Shelf, and Hollow Reef still own route memory when their run-scoped evidence exists. `HollowReefSkitter` is a second, much smaller passive scannable creature in the Hollow Reef upper-shelf pocket; scanning it records only the normal `Hollow Reef Skitter` discovery, teaches that it freezes near the upper shelf before darting away from broad return current, and can make result/recent-log memory say `Hollow Reef` without adding durable route state. `GlassfinSwarm` is a passive scannable behavior target in the wide chamber: its glassy blue route lane, small clustered bodies, and deterministic pale spacing-window pulse suggest spacing/timing in open water while leaving the lower return-current route readable, first scan records a normal `Glassfin Swarm` discovery, and repeat scan refreshes compact spacing-observation copy for free. It has no collision, damage, cargo, resource, combat, harvest, health, predator, objective, oxygen tax, tool requirement, or durable progression behavior beyond normal scan discovery. These creatures are visual/scan observations only: they keep contact collision disabled and do not damage, harvest, drop cargo, alter oxygen beyond the normal first scan cost for scannable creatures, alter progression outside scan discovery, block extraction, create a pressure gate, create a field-guide/checklist objective, or reuse predator behavior.
- Oxygen drains at `0.95` oxygen per second during an active dive. Extracting before oxygen reaches zero succeeds; reaching zero shows a placeholder failure result with cargo loss text.
- Low oxygen changes presentation only: below 25 percent, the HUD shows a low-oxygen warning; below 10 percent, the oxygen and base-direction HUD pulse with critical return feedback.
- Low-oxygen feedback does not add ascent drain, decompression, depth-based oxygen penalties, or hidden oxygen costs.
- `Burst Thruster` is a prototype non-lethal active tool mapped to Spacebar. It applies a short burst in the current movement direction, or the last facing/movement direction if no movement input is held.
- `Burst Thruster` costs 4 oxygen, has a 4-second cooldown, and cannot activate when the cost would immediately deplete oxygen.
- The HUD prompt shows whether `Burst Thruster` is ready and its oxygen cost or current cooldown. Status text reports successful activation, cooldown denial, and low-oxygen denial.
- `Burst Thruster` does not damage, stun, harvest, or bypass predators, resources, pressure locks, or progression gates.
- When `Decoy Pulse I` is relevant, the HUD prompt separately reports whether the pulse is upgrade-locked, ready on `Gulper Eel` re-scan, or already spent for the expedition. Gulper Eel scan status text reports unavailable, spent, and already-spent states, and the predator hint shifts to a temporary green decoy color while distracted.
- Current-dive state lives in `DiveSession`: oxygen, cargo, has-left-base, current depth, and dive result.
- Dives begin from an `Expedition Day N Ready` panel. Press E or Enter to begin oxygen drain and active dive play. The panel includes one compact `Today:` condition block that says why this expedition's ocean feels different without exposing seeds or implying unimplemented systems, plus one concise goal line that points to the next useful upgrade purchase, missing banked resources, scan prerequisite such as `Thermal Vent` or `Wreck Signal Cache`, or a broad route choice once configured upgrade goals are complete. Prepared lower-route players can see optional Blackwater, Dusk, Hollow Reef, wide-chamber, Mirror Kelp, Outer Shelf, or Glass Rim nudges based on existing upgrades and the latest recent route memory; these remain suggestions, not objectives, markers, maps, or checklists.
- Extraction and oxygen failure show an `Expedition Day N Result` panel summarizing the completed expedition day, banked cargo or cargo loss, one compact remembered-place line, one optional first-time major-discovery memory line, one concise route-choice callout, any compact Gulper research evidence, any recovered East Shelf signal-core payoff, durable upgrade progress, discoveries recorded or kept, best depth, and short next-expedition framing. Returning with no cargo but at least one first-time scan reports that useful scan data came home while still showing `0` banked resources; returning with no cargo and no new scan remains clearly low-value.
- If `Echo Lens I` produced a broad cache echo during the expedition, the result panel can add one compact `Research:` line preserving the weak local wreck-echo memory. This is run evidence only, not a persistent quest log, map marker, exact locator, field-guide entry, or checklist.
- `Resonance Key I` exists as a data-backed hatch-specific upgrade definition for upcoming East Shelf hatch access work. It is intentionally locked behind `Echo Lens I` plus East Shelf/Drop Echo research context and uses existing materials only. Its upgrade bay state copy uses compact route-research language such as `Recover: East Shelf or Drop Echo` and `Effect: opens East Shelf hatch only.` When owned, the current `Sealed Shelf Hatch` changes to an `OPEN` visual state by warming the hatch shimmer and softening the seal bars. `Resonance Alcove` is scaffolded as one small authored pocket just beyond the hatch with a mouth, rim, subtle glow, return-current cue, and narrow interaction that records one run-scoped hatch-echo research note. Successful extraction can acknowledge that note with one compact `Research:` line. The note does not add cargo, a new resource tier, durable route state, separate hatch save field, full interior, quest chain, minimap state, objective state, or broad key family.
- Extraction and oxygen failure result panels default to compact player-facing summaries. Development telemetry is hidden by default, but F3 or the exported `show_debug_telemetry` flag exposes result, seed, cluster pattern, expedition condition display/id, predator route, cargo collected, scans, predator contacts, oxygen at result, and failure cause. With development telemetry visible, F4 can cycle conditions such as `Calm Current` and `Thermal Bloom`, F5 can advance the current review seed, and F6 can stage Wreck Echo visual smoke states so screenshots can reproduce specific route/condition states without changing the default deterministic run flow.
- The HUD keeps a compact session-only `Recent Expeditions` log for the last three completed dives. Each entry shows result, banked cargo count, one route memory, scans, predator contacts, and best depth; seed and cluster pattern appear only while debug telemetry is enabled.
- After a successful extraction, the surface HUD uses `Result`, `Upgrades`, and `Log` views so the run result, upgrade bay, and recent-expedition log do not all compete for attention. Press Left/Right to cycle these surface views. Result/failure/log use a tall wrapped surface panel so next-expedition prompts and memory lines stay inside the frame; the `Upgrades` view keeps a compact top panel plus a separate upgrade details panel with compact effect/feedback copy that stays inside the `1280x720` viewport.
- The `Result` view keeps the extraction summary in focus, the `Upgrades` view focuses the data-backed upgrade bay with Up/Down selection and E or Enter purchase, and the `Log` view shows the recent expedition history in the run panel. The upgrade surface summary and panel title both keep the Up/Down selection affordance visible without adding a tech tree, inventory grid, or broader upgrade UI.
- Surface states are panel-led: ready/result/failure screens hide loose title/instruction labels and operational dive HUD rows such as oxygen, depth, base distance, cargo, scan target, discoveries, and status so they do not overlap the ready/result panels. The ready panel owns movement and start instructions; the `F9` prototype reset hint appears only while debug telemetry is visible.
- Active dives show only the compact operational HUD: `O2`, depth, base direction, one-line cargo, compact discovery count, cargo slots, and bounded route/prompt/status text. The active HUD now follows the reference direction of compact separated edge instruments: top-left dive instruments with oxygen/depth bars and icons, top-center cargo capacity, top-right base needs with colored icons, a bottom-center tool belt, a left route objective card, a left depth rail, and a bottom-right schematic minimap. The right-side scan-target card and world-space scan reticle are contextual popups: they appear only while a scan target is selected. These active HUD labels use reserved rows inside their panels; prompt/status/scan text clips inside its row instead of wrapping into neighboring labels, so adding a future command, lock read, scan note, or active tool should happen through the shared active-HUD layout helper rather than free-floating label offsets. The active HUD uses lighter translucent glass, tighter shadows, smaller active-dive labels, subtle cyan/blue-gray borders, cargo slots matching the current usable capacity, and a separate warm warning panel for low/critical oxygen. Banked resources, upgrade details, recent expedition history, and full discovery lore remain out of the loose dive HUD.
- Each expedition has a session number and deterministic seed. The raw seed is visible only when development telemetry is enabled.
- Each expedition now selects a deterministic expedition condition from the current seed. The condition has an id, display name, short briefing line, and tags. Most condition effects are still readability/presentation effects: `Calm Current` subtly emphasizes safe-return lane/rib/beacon visuals, `Low Visibility` compactly warns that lower-trench routes are murkier and strengthens the Dusk Trench `LowVisibilityCue`, and `Rare Signal` adds only faint Wreck Shelf, East Shelf, Blue Chimney, and preparation-gated Blackwater signal shimmers that are not markers, objectives, locators, cache reveals, or reward guarantees. `Rare Signal` also gently brightens the midwater `RESEARCH ROUTE` lane while keeping the `SAFE BANK` lane visible, nudging route preference without locking routes or guaranteeing rewards. When all current upgrade goals are complete, the ready-panel goal can suggest banking Shell Reef or trying Shelf Drop and Blue Chimney if oxygen allows; after `Resonance Key I` is owned, it can suggest the Blackwater lower route, and recent Blackwater/Dusk/Hollow Reef route memory can nudge the next dive toward Dusk, Hollow Reef, or the wide chamber. `Rare Signal` can still broadly suggest checking the Blackwater edge signal if oxygen allows, then returning safely. `Thermal Bloom` adds a subtle warmth/bubble emphasis to the Thermal Vent visuals and narrowly biases `Glow Plankton` selection toward existing authored vent-pocket candidates when those candidates are available. Conditions do not save active expedition state and do not change predator routes, oxygen, scan range, pressure gates, cargo limits, resource counts, movement, upgrades, or broad region layout.
- Each expedition selects a seeded resource cluster pattern: `Cautious shallows` or `Deep reward route`. The raw pattern label is visible only when development telemetry is enabled.
- Pressing R after a result prepares the next seeded expedition, advancing the session number and seed while preserving banked resources, upgrades, discoveries, and best depth. The ready status says the new expedition is ready and that the ocean changed overnight.
- Session progression lives in `ProgressionState`: banked resources, purchased upgrades, scan discoveries, and best depth reached.
- Long-term progression automatically loads from one local prototype save slot at launch and saves after extraction, oxygen failure, scanning, and upgrade purchase.
- The prototype save uses schema version `1`. It includes `save_version`, banked resources keyed by resource id, purchased upgrades keyed by upgrade id, scan discoveries keyed by discovery id, and best depth reached.
- Scan discovery entries still keep a small display snapshot for the current practical scan-result UI, but progression identity should continue to come from discovery ids and upgrade ids rather than copied display text.
- Missing `save_version` saves are treated as legacy version `0` and migrate by loading the known progression fields. Current version `1` loads the same known fields directly. Unknown newer versions warn and load known fields only so prototype progress is not discarded.
- The prototype save does not restore active oxygen, current cargo, active run state, temporary effects, current expedition number, current expedition seed, or whether the one-use `Decoy Pulse I` has been spent during the active expedition.
- Pressing `F9` resets the local prototype save from inside the running game. It clears banked resources, purchased upgrades, scan discoveries, best depth, and the save file, then returns the scene to a clean Expedition 1 ready state. The control remains available during development, but normal player-facing ready-panel copy hides it unless debug telemetry is visible.
- Use `F9` before first-run playtests when the tester should experience the prototype without previously owned upgrades, banked resources, scan discoveries, or best-depth history. Relaunching the game without `F9` should keep durable progression but still start from a fresh ready expedition rather than an in-progress dive.
- This means owned upgrades surviving a close/relaunch is intentional. The save keeps durable progression, while active expedition state such as current oxygen, carried cargo, active result/dive state, temporary Decoy Pulse spent state, and current run-in-progress context is not restored.
- Extraction only succeeds after the player has left the base and returned.
- The player starts with three cargo slots. Resource pickups fill cargo during a dive and cost 1 oxygen to collect.
- The active dive HUD shows cargo as a compact count plus slot placeholders: filled slots tint by carried resource and show tiny resource mini-icons, empty slots show remaining capacity, and the fourth slot stays hidden until `Cargo Rack I` raises capacity. This is a readability treatment only and does not change collection, extraction, failure, or banking behavior.
- Starter resources are placed from authored candidate points using the current run seed: `Kelp Fiber` stays shallow, `Shell Fragments` stays midwater, and `Glow Plankton` stays deep. The Hollow Reef wide chamber now contributes one optional deep-reward `Shell Fragments` position near the sealed salvage cache so a run can ask for cargo-vs-knowledge judgment without adding pickup count. The `Thermal Vent` pocket and `Blue Chimney` lower pocket contribute deep `Glow Plankton` candidates for existing seeded selection rather than adding extra active resource pickups; on `Thermal Bloom` expeditions, authored vent-pocket `Glow Plankton` candidates are preferred without increasing pickup count.
- Starter resource candidate points use typed `SpawnPoint` nodes with category, target id, depth band, and position data so later hazards, creatures, discoveries, and resource clusters can use the same placement model. The first Area 01 resource pass groups starter candidates into readable terrain pockets: Food/Water/Driftwood/Kelp appear around shallow left and right shelf pockets, Scrap and Shell Fragments teach a left cave material pocket, and Quartz/Shell placements sit farther down in the right-side material pocket so resource depth starts to explain where the player is exploring.
- The main scene includes passive `LandmarkMetadata` nodes for Surface Base, Shell Reef, East Shelf Arch, Drop Arch, Blue Chimney, Silt Vein Fork, Dusk Trench, Hollow Reef, Thermal Vent Field, Wreck Shelf, Pressure-Locked Research Wreck, and Gulper Route. These describe persistent place identity for future planning/review but do not change current gameplay behavior. Each metadata node now exposes a stable region name and a memory goal; the pressure-locked wreck keeps its specific display name while sharing `Wreck Shelf` as its remembered region. Result summaries use existing run evidence to show one remembered-place line, prioritizing lower-route run evidence, Gulper Route, Wreck Shelf, Thermal Vent Field, Shell Reef, then Surface Base fallback without adding map UI or exact location tracking.
- `Cautious shallows` clusters keep the first three starter resources closer to safer shallow/midwater banking routes, while `Deep reward route` clusters pull the deeper `Glow Plankton` toward the predator-controlled route.
- `Deep reward route` runs show a faint glow/current lure with a compact reward core below the `Shell Reef` decision band, hinting at valuable deep `Glow Plankton` after the player can read the safer midwater bank route.
- A compact authored `Shell Reef` pocket sits in the midwater band as a pale ribbed shell/coral route landmark with a safer return-current cue. It uses the first source/export pocket landmark sprite at `assets/source/sprites/landmark_shell_reef_pocket_sprite_v1.svg` and `assets/exports/sprites/landmark_shell_reef_pocket_sprite_v1.svg`, with fallback geometry still visible for route readability and provenance in `assets/licenses/landmark_shell_reef_pocket_sprite_v1.txt`. It keeps typed `Shell Fragments` spawn candidates, but it does not add extra pickup count, collision, a new resource tier, a new predator, or procedural biome behavior.
- The `Shell Reef Shelf` is scannable as an environmental clue. It uses the first source/export scan-target sprite at `assets/source/sprites/landmark_shell_reef_shelf_scan_sprite_v1.svg` and `assets/exports/sprites/landmark_shell_reef_shelf_scan_sprite_v1.svg`, with provenance in `assets/licenses/landmark_shell_reef_shelf_scan_sprite_v1.txt`, while keeping fallback and scan-marker nodes available. First-time scan records that the reef is a safer midwater banking route before deeper pressure and predator risk; repeat scan remains free and refreshes the compact route clue.
- The `Gulper Eel` predator route is selected from authored creature route spawn points by expedition seed and cluster pattern. Cautious patterns choose a deep left or right gate, while deep-reward patterns choose an upper or lower predator gate near the deeper reward route.
- The predator warning current/marker moves with the selected route, and result telemetry records the selected predator route id for playtest comparison.
- A visible `Pressure-Locked Research Wreck` sits behind a clearer locked-route pressure gate near the lower midwater/deep transition. The gate uses blue rails, crossbars, shimmer, and a compact `LOCKED` badge while sealed, then softens those bars to a green passable `OPEN` state after `Pressure Seal I` is owned. It safely denies entry until the upgrade is owned, without adding hidden oxygen damage.
- The wreck has an outside scan target, `Pressure-Locked Research Wreck`, that can be scanned before entry. It uses the first source/export outside-wreck scan sprite at `assets/source/sprites/pressure_wreck_outer_scan_sprite_v1.svg` and `assets/exports/sprites/pressure_wreck_outer_scan_sprite_v1.svg`, with provenance in `assets/licenses/pressure_wreck_outer_scan_sprite_v1.txt`. Scanning it records a practical clue to buy `Pressure Seal I` at the surface and then return; it also reveals a subdued future-cache signal so the cache reads as a later payoff, not an immediate open path.
- After `Pressure Seal I` is owned, the wreck alcove can be reached and the inside `Wreck Signal Cache` can be scanned as a practical progression reward pointing toward a future scanner improvement. It uses the first source/export cache sprite at `assets/source/sprites/wreck_signal_cache_sprite_v1.svg` and `assets/exports/sprites/wreck_signal_cache_sprite_v1.svg`, with provenance in `assets/licenses/wreck_signal_cache_sprite_v1.txt`. The wreck/cache mystery signal is a remembered-place hook: it implies deeper wreck transmissions and future access planning, but it does not add coordinates, a quest checklist, a map marker, a field-guide entry, or a guaranteed safe route.
- Extraction banks carried resources into session progression. Oxygen failure discards carried resources but keeps banked resources.
- After extraction, the surface upgrade bay view shows data-backed upgrade entries with cost, one-line missing resources, missing discovery if any, and explicit `State:` labels for owned, available, scan-locked, and missing-resource upgrades. `Echo Lens I` adds one compact role hint, `broad wreck echoes, not material pings`, so it reads differently from `Signal Lens I` without becoming a tech tree. Extraction feedback also calls out newly ready upgrades when banked cargo completes a cost.
- The upgrade bay supports selecting configured upgrades with Up/Down while extracted. The current prototype has eight configured upgrades: `Oxygen Tank I`, `Pressure Seal I`, `Signal Lens I`, `Echo Lens I`, `Resonance Key I`, `Cargo Rack I`, `Predator Warning I`, and `Decoy Pulse I`. Selected-upgrade details use short effect summaries and compact feedback so cost, state, missing requirements, and the last purchase/banking message fit in the one-selected-upgrade panel without adding a tech tree or inventory grid.
- `Oxygen Tank I` can be bought from the upgrade panel with banked `Kelp Fiber x2`, `Shell Fragments x1`, and `Glow Plankton x1`.
- `Oxygen Tank I` raises future dive max oxygen from 30 to 40 during the current session.
- `Pressure Seal I` requires the `Thermal Vent` discovery plus banked `Kelp Fiber x1`, `Shell Fragments x2`, and `Glow Plankton x2`.
- `Pressure Seal I` opens the first pressure-locked research wreck route by disabling the pressure-boundary denial and changing the shimmer feedback to show the route is open.
- `Signal Lens I` requires the `Wreck Signal Cache` discovery plus banked `Kelp Fiber x1`, `Shell Fragments x2`, and `Glow Plankton x2`.
- `Signal Lens I` makes repeat resource scans add a short local signal hint toward the nearest matching visible uncollected deposit in the current expedition. If no matching deposit is visible, the HUD says the signal is quiet.
- After `Signal Lens I` is owned, repeat-scanning `Wreck Signal Cache` gives a compact unresolved `cache echo` hint toward future `Echo Lens` study and deeper wreck signals. This is only planning language for the next scanner curiosity; it does not add a minimap, field guide, exact-coordinate tracker, objective checklist, or new resource-pulse behavior.
- `Echo Lens I` requires the `Wreck Signal Cache` discovery, ownership of `Signal Lens I`, and banked `Kelp Fiber x1`, `Shell Fragments x2`, and `Glow Plankton x3`. After ownership, repeat-scanning known `Wreck Signal Cache` returns one broad local curiosity line, `Echo Lens: weak wreck echo lingers below the shelf,` and briefly plays a local scan/echo pulse on the cache. This does not add minimap, field guide, exact-coordinate tracking, scanner energy, hidden object reveal, persistent marker, or objective checklist behavior.
- `Cargo Rack I` costs banked `Kelp Fiber x2`, `Shell Fragments x2`, and `Glow Plankton x1`.
- `Cargo Rack I` raises future dive cargo capacity from 3 to 4 without changing extraction banking or the rule that oxygen failure loses all carried cargo.
- `Predator Warning I` requires the `Gulper Eel` discovery plus banked `Kelp Fiber x1`, `Shell Fragments x2`, and `Glow Plankton x1`.
- `Predator Warning I` makes existing predator warning feedback begin farther from the `Gulper Eel` without changing patrol, chase/contact radius, oxygen loss, knockback, or seeded route selection.
- `Decoy Pulse I` requires the `Gulper Eel` discovery, ownership of `Predator Warning I`, and banked `Kelp Fiber x1`, `Shell Fragments x1`, and `Glow Plankton x2`. After purchase, re-scanning the `Gulper Eel` with F spends one decoy pulse for the current expedition, briefly pulling the predator away from the player to create a temporary route-timing window. The effect does not damage, stun-lock, harvest, trap, remove, or permanently control the predator, and it does not change seeded route selection, pressure locks, extraction, cargo failure rules, or `Burst Thruster` behavior.
- Scan with F. The HUD shows the current scan target name plus compact metadata such as `new resource`, `known creature`, `new environment`, or `known wreck signal`; the selected target is highlighted. Non-resource scan targets use a compact pale diamond marker with focus brackets when selected, while resource pickups keep their material silhouettes and selected-target tint. Targeting chooses the nearest valid scan target, with stable id-based tie-breaking when distances match, then keeps the current target inside a small buffer so the scan reticle does not jump between nearby targets while the player is still reading it.
- First-time scans cost 2 oxygen and record session-persistent discoveries. First-time scans of major route/research targets such as `Thermal Vent`, `Shell Reef Shelf`, pressure wreck signals, `Wreck Signal Cache`, `Gulper Eel`, and passive route creatures can add one compact result memory line. Re-scanning an already discovered target does not spend oxygen, does not add duplicate result memory, but still refreshes that discovery's practical effect if it has one.
- Current planning recommendation keeps repeat tactical scans free for this prototype; see `docs/planning/REPEAT_SCAN_COST_RECOMMENDATION.md`.
- First-time scan status text includes a concise next action. Resource scans point the player to collect the resource and return to base to bank cargo, while non-resource scans point toward the revealed clue or a safe return.
- Resource pickups are scannable before collection. Resource scans record the resource depth band, upgrade use, and whether more of that material is needed for `Oxygen Tank I`; they also temporarily highlight matching visible resource deposits in the current expedition. Resource selected/highlight/idle tinting uses the reusable `ScanPulseVisual` helper so the temporary pulse is consistent without becoming a permanent map marker or exact tracker.
- Resource pickups use distinct prototype silhouettes: `Kelp Fiber` reads as green fronds/stem, `Shell Fragments` as pale ribbed shell shards, and `Glow Plankton` as a bioluminescent core/halo bloom. `Glow Plankton` uses the first source/export resource asset at `assets/source/sprites/resource_glow_plankton_sprite_v1.svg` and `assets/exports/sprites/resource_glow_plankton_sprite_v1.svg`, with provenance in `assets/licenses/resource_glow_plankton_sprite_v1.txt`. `Kelp Fiber` uses the first source/export resource asset at `assets/source/sprites/resource_kelp_fiber_sprite_v1.svg` and `assets/exports/sprites/resource_kelp_fiber_sprite_v1.svg`, with provenance in `assets/licenses/resource_kelp_fiber_sprite_v1.txt`. `Shell Fragments` uses the first source/export resource asset at `assets/source/sprites/resource_shell_fragments_sprite_v1.svg` and `assets/exports/sprites/resource_shell_fragments_sprite_v1.svg`, with provenance in `assets/licenses/resource_shell_fragments_sprite_v1.txt`. Their old polygon groups remain hidden as fallbacks. These visual differences do not change pickup count, collision, scan behavior, or resource identity.
- Pickups, major landmarks, and key scannable objects now follow the same placeholder-to-asset workflow as the player and surface base: each target has a `SpriteAnchor/Sprite` slot for future PNG art, while the current polygon read remains grouped under `FallbackVisual` or `FallbackGeometry`. Resource pickup visuals are child scene instances: `scenes/resources/KelpFiberVisual.tscn`, `scenes/resources/ShellFragmentsVisual.tscn`, and `scenes/resources/GlowPlanktonVisual.tscn`. The Shell Reef pocket visual geometry is instanced from `scenes/readability/ShellReefPocketVisuals.tscn`. Area 01 terrain dressing now uses reusable `scenes/environment/Area01ReefWallKit.tscn` and `scenes/environment/Area01ResourcePocketKit.tscn` instances on the main shell walls, ledges, and resource pockets; these are visual-only modules and do not own collision, scan, resource, oxygen, or progression behavior. Resource `Area2D` nodes, definitions, collision shapes, scan groups, spawn placement, and cargo behavior remain behavior-facing in `Main.tscn`, so replacing placeholder art should not require scanner, collection, or pressure-gate logic changes.
- `Lantern Fry` is a passive scannable creature near the deeper `Glow Plankton`; scanning it temporarily pulses nearby `Glow Plankton` and suggests following the pulse if oxygen allows. It now uses the first source/export creature sprite at `assets/source/sprites/creature_lantern_fry_sprite_v1.svg` and `assets/exports/sprites/creature_lantern_fry_sprite_v1.svg`, with provenance in `assets/licenses/creature_lantern_fry_sprite_v1.txt`. The old body polygon is hidden as a fallback reference, while the existing glow fallback remains active for the subtle visual-only idle bob/glow pulse. Its movement route, scan target behavior, discovery data, and glow-plankton pulse effect are unchanged.
- The `Glassfin Swarm` is a passive scannable creature-route read in the Hollow Reef wide chamber. A scan can add compact result and Recent Expeditions memory about reading the swarm spacing window without fighting, while higher-priority route evidence such as Hollow Reef readings and Dusk reach still wins. This memory is session/log copy only and does not add durable quest state, a monster journal, creature inventory, combat progression, harvests, damage, oxygen tax, or a field-guide/checklist UI.
- A scannable `Thermal Vent` sits inside a small authored warm-current pocket with vent wash, cracked shelf, dark vent mouth, paired chimney silhouettes, heat-plume core, bubble strings, and a subtle current ribbon. The pocket visual geometry is instanced from `scenes/readability/ThermalVentPocketVisuals.tscn`; the Thermal Vent scan target uses the first source/export landmark sprite at `assets/source/sprites/landmark_thermal_vent_sprite_v1.svg` and `assets/exports/sprites/landmark_thermal_vent_sprite_v1.svg`, with provenance in `assets/licenses/landmark_thermal_vent_sprite_v1.txt`. The Thermal Vent `Area2D` scan target, route reveal, and condition visual sync remain owned by the existing gameplay nodes. Scanning it reveals a soft optional-current hint without adding another active `Glow Plankton` pickup; vent-adjacent glow remains handled by seeded starter-resource selection.
- `Wreck Echo Descent` is now implemented as one deeper-right authored route pocket, not a broad biome or generated map layer. The pocket sits slightly lower/right of the pressure-cache and predator/reward stack and uses a pale blue-white echo treatment so it reads as research curiosity rather than a green resource lure or red danger lane. The clue marker uses the first source/export vector reference at `assets/source/sprites/wreck_echo_clue_marker_v1.svg` and `assets/exports/sprites/wreck_echo_clue_marker_v1.svg`, with provenance in `assets/licenses/wreck_echo_clue_marker_v1.txt`; the runtime scene mirrors that source-art shape with scene-native `Polygon2D` children under the existing clue trigger so no generated import metadata is required. The older polygon clue core/ring remain subdued fallback readability geometry. The pocket's clue trigger becomes active only after the player owns both `Pressure Seal I` and `Echo Lens I`; `Pressure Seal I` still controls pressure access, and `Echo Lens I` does not bypass the gate. Touching the clue during a dive marks a run-scoped clue, disables that clue trigger for the rest of the expedition, and asks the player to return to base. Successful extraction adds one compact result line: `Research: Wreck Echo clue carried a deeper pressure signal below the shelf.` Returning without the clue adds no Wreck Echo line, oxygen failure adds no special Wreck Echo punishment, and the clue flag resets on the next expedition. This feature adds no minimap, exact locator, field guide, quest checklist, combat, new economy, durable Wreck Echo save data, or persistent objective marker.
- A `Gulper Eel` predator patrols a deep route to a valuable `Glow Plankton` pickup. The route has red warning-current edges, rib marks, an eye, and a compact bite/hazard badge, while the predator silhouette uses the first source/export predator sprite at `assets/source/sprites/creature_gulper_eel_sprite_v1.svg` and `assets/exports/sprites/creature_gulper_eel_sprite_v1.svg`, with provenance in `assets/licenses/creature_gulper_eel_sprite_v1.txt`. The grouped fallback tail/body/jaw/spine/eye shapes remain available as state/readability overlays. Patrol, warning, chase, and decoy states tint the fallback body/eye and patrol hint for clearer readability, but detection, route selection, contact oxygen cost, knockback, decoy behavior, and scan behavior are unchanged. Getting too close triggers a brief chase; contact costs 5 oxygen, knocks the player back, and briefly disrupts movement.
- The `Gulper Eel` is scannable as a non-combat observation target. First-time scan records a durable behavior clue about warning currents, patrol hints, oxygen risk, and route timing; repeat scans remain free and refresh the predator-route warning text without damaging, slowing, stunning, repelling, harvesting, or controlling the predator.
- Gulper route evidence remains session-only for the current prototype. A dive that scans the `Gulper Eel`, suffers predator contact, or spends `Decoy Pulse I` can add one compact `Research:` line to the result summary, such as route timing, warning-lane danger, or decoy timing. This does not add weapons, harvests, health, capture, permanent predator control, or a field-guide UI.
- The HUD shows current depth, best depth reached, and base direction/distance so the return route remains understandable while diving. From expanded side routes, base direction can include horizontal orientation such as `up-left` plus compact broad route-chain hints like `Silt/Blue`, `Dusk/Blackwater`, or `Hollow/Dusk/Blackwater` so return guidance stays useful without a minimap, breadcrumbs, exact coordinates, or a checklist. Oxygen uses normal, low, and critical player-facing states: low oxygen adds inline `LOW` text plus a compact upper-right warning panel, while critical oxygen adds inline `CRITICAL`, stronger color/pulse treatment, and return-to-base emphasis without changing oxygen costs, failure thresholds, upgrades, or save/load behavior.
- The starter player is now a compact diver-scale avatar rather than a bulky submarine. `VisualRoot/SubSpriteAnchor/SubSprite` carries the generated six-frame swim sheet at `assets/sprites/player/player_diver_swim_sheet_v1.png`, with source/provenance files at `assets/source/sprites/player_diver_swim_sheet_source_v1.png` and `assets/licenses/player_diver_swim_sheet_v1.txt`. Movement advances the sheet through region frames so the flippers/body animate while the idle state returns to a stable readable frame. The player physics root stays unmirrored during direction changes; only `VisualRoot` flips left/right so collision, camera, movement, and gameplay state remain stable. The diver uses a smaller collision capsule and a pulled-back camera so the surrounding ocean has more room to read. Idle and movement states remain visual-only: `IdleBubble` appears while still, while `BubbleTrail` brightens and `ThrustFlare` appears while moving. The earlier polygon hull, viewport, scanner mount, fins, and cabin-light fallback placeholders remain hidden under the sprite slot as replaceable reference geometry. The compact submarine fantasy moves to future upgrades rather than the starting avatar.
- The surface base uses the boat sprite at `assets/sprites/base/research_boat_clean.png` plus a wired moonpool/waterline source-art slot at `assets/exports/sprites/surface_moonpool_waterline_v1.svg`. The moonpool/waterline sprite sits under `SurfaceBaseArt/MoonpoolGuide` and keeps the existing fallback shadow, launch column, glow, and foam shapes subdued but available for readability tuning. `BaseZone` remains the separate gameplay start/extraction area, now marked by a visible under-ship dock core/rim below the hull so extraction reads as entering the moonpool rather than an invisible trigger zone.
- The surface base uses the clean continuous-hull generated research boat/lab sprite at `assets/sprites/base/research_boat_clean.png` under `SurfaceBaseArt/SpriteAnchor/ResearchBoatSprite`. A subtle `SurfaceBaseArt/MoonpoolGuide` sits behind the boat sprite to clarify the waterline and launch column without covering the hull. Earlier generated boat variants remain in `assets/sprites/base/` as source/reference assets, while the prior polygon boat, cabin, beacon, moonpool, and moonpool-light remain hidden as fallback layout references under `SurfaceBaseArt/FallbackGeometry`; `BaseZone` remains the separate extraction/start gameplay area. The player sub starts and clamps below the hull so ascending to extract cannot visually park the sub on top of the boat sprite.
- Lower-route world labels are intentionally sparse. The active HUD/prompt owns current interaction text and base-return language, while normal-play scene labels are reserved for compact place names or locked/open promise states. Redundant `RETURN VIA...` labels in the Wide Reef / salvage / Mirror Kelp cluster are hidden by default so they can remain available for future review without crowding the screen.
- The scene uses a first-pass `Area01ArtSlice` layer stack for normal-play environment readability: far and mid background masses, a reusable `OceanLightingStack`, surface light shafts, opaque terrain back walls, matching terrain collision, lit ledge edges, gameplay-object slots, and foreground decor. The ocean lighting stack adds a brighter surface wash, stronger blue depth haze, subtle center play-space light, caustic ribbons, and edge/lower vignettes so the active-dive space reads less flat while keeping terrain, cargo, and the player readable. The earlier broad `SafeShallowsExplorationShell` remains in the scene as historical scaffolding but is hidden by default because it read like overlapping route/debug geometry at play scale. The active dive HUD now uses one compact `SURVIVAL ROUTE` objective card with a short objective line plus reserved prompt/status rows; scan cards stay contextual and hidden when no target is selected. The scene also uses quieter surface light rays, subtle but readable depth transition bands, a more readable surface boat/lab extraction origin, a muted shallow/midwater background study, subdued shallow kelp silhouettes, a quieter midwater shelf and wreck silhouette, tuned Shell Reef landmark wash/shell silhouettes and return-current shapes, a warmer Thermal Vent landmark silhouette with chimney/mouth/plume cues, subdued cyan/green route-current hints, lighter deep pressure haze, and a restrained lower-route cue stack where broad deep-reward haze, scan/cache halos, Rare Signal wash, predator lane fills, Shell Reef fallback geometry, Gulper overlay shapes, pressure gate bars, and the first Wreck Echo pocket stay quieter than their core markers. The Area 01 Hollow Reef / Wide Reef / Mirror Kelp / salvage-pocket rescue pass also keeps large cave water, shelf silhouettes, kelp curtains, return-current bands, opened-pocket support shapes, and Calm Current/Kelp Bloom condition nudges below pickup and knowledge-payoff brightness, so the sub, resource sprites, Tideglass Sample, Salvage Data Cache, Salvage Manifest, and lock reads remain the visual priority during normal play. The pressure badge, cache core, amber deep-reward lure sprite, predator marker, and Wreck Echo clue core remain distinct so blocked opportunity, local mystery, reward lure, route danger, and deeper research can be read without adding broad map mechanics. The shallow/midwater background study uses source/export/provenance files at `assets/source/sprites/shallow_midwater_background_study_v1.svg`, `assets/exports/sprites/shallow_midwater_background_study_v1.svg`, and `assets/licenses/shallow_midwater_background_study_v1.txt`; it is background support only and does not change gameplay nodes, spawn points, route cues, HUD, collision, or scan behavior. The pressure wreck/cache shell uses source/export/provenance files at `assets/source/sprites/pressure_wreck_cache_shell_sprite_v1.svg`, `assets/exports/sprites/pressure_wreck_cache_shell_sprite_v1.svg`, and `assets/licenses/pressure_wreck_cache_shell_sprite_v1.txt`; it is environment-shell support only and does not own Wreck Echo gameplay state. The deep reward lure uses source/export/provenance files at `assets/source/sprites/deep_reward_lure_sprite_v1.svg`, `assets/exports/sprites/deep_reward_lure_sprite_v1.svg`, and `assets/licenses/deep_reward_lure_sprite_v1.txt`; it is visual support for optional material opportunity only and does not add resources, routes, scans, progression, or objective UI. The Shell Reef pocket, Thermal Vent pocket, and pressure wreck hull/gate visual clusters are instanced from `scenes/readability/`; gameplay ownership remains in `Main.tscn` through spawn candidates, scan targets, route reveals, the pressure boundary, Wreck Echo trigger, upgrade state, and scan/cache nodes.

## Runtime / Setup

- Engine: Godot 4.7 with GDScript.
- Primary target: local desktop.
- Secondary target: optional web demo. The project uses Godot's Compatibility renderer so web export remains plausible; avoid Godot C# unless the web target is intentionally abandoned.
- Launch locally from the repository root:

```powershell
& "C:\Program Files\Godot\Godot_v4.7-stable_windows_arm64.exe" --path .
```

- Headless launch smoke from the repository root:

```powershell
& "C:\Program Files\Godot\Godot_v4.7-stable_windows_arm64_console.exe" --path . --headless --quit-after 1
```

- Source layout:
  - `project.godot`: Godot project configuration.
  - `scenes/Main.tscn`: first scene.
  - `scenes/Player.tscn`: prototype diver scene with a sprite-ready `VisualRoot`, generated compact `SubSprite`, hidden polygon fallback, and separate collision.
  - `scripts/main.gd`: scene orchestration, HUD updates, expedition flow, and scene-specific scan/upgrade effects.
  - `scripts/dive_session.gd`: current-dive oxygen, cargo, depth, extraction, and failure state.
  - `scripts/progression_state.gd`: session-persistent resources, upgrades, discoveries, best depth, and save schema migration.
  - `scripts/player.gd`: basic placeholder player movement.
  - `scripts/resource_definition.gd`: typed resource data definition for upcoming pickup/resource work.
  - `scripts/resource_pickup.gd`: pickup nodes that emit collection events and reset between dives.
  - `scripts/scannable.gd`: simple scannable targets, including passive moving creatures.
  - `scripts/spawn_point.gd`: typed candidate placement points for seeded resource and creature route variation.
  - `scripts/spawn_selection.gd`: deterministic helper for seed-to-pattern selection, spawn positions, and authored route dictionaries.
  - `scripts/expedition_condition.gd`: deterministic presentation data for seeded expedition-day conditions.
  - `scripts/landmark_metadata.gd`: passive authored metadata for persistent place identity.
  - `scripts/scan_target_resolver.gd`: deterministic helper for scan target validity, identity, display fields, and nearest-target tie-breaking.
  - `scripts/predator.gd`: simple patrol, detection, chase, and contact behavior.
  - `scripts/upgrade_definition.gd`: small data model for upgrade definitions and generic purchase handling.
  - `scripts/upgrade_purchase.gd`: generic upgrade prerequisite and purchase gate helper.
  - `tests/logic_tests.gd`: headless deterministic checks for cargo, extraction, oxygen failure, upgrades, save/load, seeds, spawn matching, and discovery prerequisites.
  - `resources/*.tres`: starter resource definitions.
  - `resources/upgrades/*.tres`: upgrade definitions.
- GitHub Actions:
  - `.github/workflows/godot-smoke.yml`: runs a Godot headless launch smoke check, deterministic logic tests, and `git diff --check`.
- Do not commit `.godot/`, `.import/`, `*.import`, local editor cache, build output, secrets, or export artifacts.

## Verification

Preferred checks:

```powershell
& "C:\Program Files\Godot\Godot_v4.7-stable_windows_arm64_console.exe" --path . --headless --quit-after 1
& "C:\Program Files\Godot\Godot_v4.7-stable_windows_arm64_console.exe" --path . --headless --script res://tests/logic_tests.gd
git diff --check
```

Manual smoke:

- Launch the project and confirm the first scene runs.
- Confirm the expedition starts at the `Expedition Ready` panel and does not begin active oxygen pressure until E or Enter is pressed.
- For clean first-run checks, press `F9` on the ready screen and confirm the ready panel returns to `Expedition 1`, oxygen is back to the base starting value, banked resources/upgrades/discoveries are cleared, and best depth is reset.
- Confirm the ready-panel goal keeps upgrade resource and scan-prerequisite goals ahead of the `Shell Reef` route objective.
- Confirm each restarted expedition advances the session number and seed while persistent progression remains.
- Relaunch the project after banking resources, buying an upgrade, or recording a discovery, and confirm long-term progression reloads while active cargo and current-dive state do not.
- Relaunch the project after pressing `F9` and confirm the clean progression state persists across launch.
- Confirm restarted expeditions vary the starter resource positions while keeping `Kelp Fiber` shallow, `Shell Fragments` midwater, and `Glow Plankton` deep.
- Confirm restarted expeditions alternate between `Cautious shallows` and `Deep reward route` cluster patterns, and that the deep reward pattern tempts a route near the predator patrol.
- Confirm restarted expeditions vary the `Gulper Eel` route and move the warning current/marker with it without blocking the safe return path.
- Confirm the deep-reward lure appears only on `Deep reward route` expeditions, begins below the `Shell Reef` decision band, and preserves surface return readability.
- Confirm the `Shell Reef` pocket is readable as a midwater landmark, leaves open route gaps, can host the existing single `Shell Fragments` pickup, and uses the return-current shapes to point back toward the surface/base path without blocking it.
- Scan the `Shell Reef Shelf`, confirm discovery/result/log text records a safer midwater bank route clue, then repeat scan and confirm no oxygen is spent.
- After extraction, use Left/Right to cycle the compact surface views and confirm `Result`, `Upgrades`, and `Log` keep their text readable; confirm Up/Down and E or Enter still select and purchase upgrades only while the `Upgrades` view is active.
- Move or edit a `StarterResourceCandidates` spawn point in the scene and confirm seeded resource placement follows that typed definition.
- Move the placeholder submersible with WASD or arrow keys and confirm it accelerates, slows under drag, turns toward velocity, dives downward from the surface, and stays inside the test bounds.
- During an active dive, press Spacebar and confirm `Burst Thruster` moves the sub in the held movement direction, spends 4 oxygen, starts a 4-second cooldown, and updates HUD/status feedback.
- Try Spacebar again during cooldown and confirm no oxygen is spent and the status shows remaining cooldown.
- Try Spacebar with 4 or less oxygen and confirm it does not activate or immediately fail the dive.
- Use Spacebar near the predator route and near the pressure-locked wreck; confirm it can reposition the player but does not damage predators or bypass the pressure lock.
- Return to the safe base, press E or Enter, and confirm the HUD shows a successful extraction result.
- Confirm extraction and oxygen failure both show result summaries before restarting.
- Confirm extraction and oxygen failure summaries hide raw telemetry by default, then press F3 and confirm seed, pattern, condition display/id, predator route, cargo, scans, predator contacts, oxygen result, and failure cause appear for playtesting.
- Press F4 and F5 while debug telemetry is hidden and confirm nothing player-facing changes. Press F3, then use F4 to cycle through visible condition states including `Calm Current` and `Thermal Bloom`, and use F5 to advance the current review seed while telemetry reports the updated seed, pattern, and condition.
- Confirm extraction and oxygen failure summaries include at most one compact `Route choice:` line when run evidence supports it, such as reef banking, East Shelf / Shelf Drop / Blue Chimney / Blackwater / Dusk / Hollow Reef / Wide Reef Chamber research push, Thermal Vent clue progress, passive Lantern Ray observation, deep glow push, predator-route pressure, or pressure-wreck progress, without turning the panel into a checklist; the Recent Expeditions log should also show one compact `route <place>` memory while hiding seed/pattern telemetry until debug telemetry is enabled.
- For repeatable exported-browser screenshot smoke, run `npm run test:visual` to capture surface ready, active dive, extraction result, upgrade tab, lower-route pressure-gate, staged Dusk/Hollow/Mirror Kelp lower-route views, and other named route readability views. Use this as visual evidence alongside, not instead of, Godot headless logic tests.
- Complete or fail four expeditions and confirm the `Recent Expeditions` log shows only the latest three results, with seed and cluster pattern hidden until debug telemetry is enabled.
- Approach multiple nearby scan targets and confirm the HUD names one selected target with a visible highlight, then confirm the selected target remains deterministic until distance changes.
- Approach a new resource, already discovered resource, creature, environmental scan target, and wreck signal; confirm the scan target HUD metadata stays compact and accurate, non-resource scan markers remain readable, and resource pickups still read as cargo first.
- Confirm immediate extraction at the starting base does not succeed until the player leaves and returns.
- Confirm oxygen decreases during the active dive.
- Confirm low oxygen shows warning feedback below 25 percent and stronger base-direction/oxygen emphasis below 10 percent without changing oxygen costs.
- Collect resources, confirm cargo fills up to three slots, extract, and confirm resources move into the banked list.
- After extraction, confirm the surface upgrade bay shows a selected upgrade entry, cost, one-line missing requirements, owned/available/locked/missing-resource state, and feedback.
- Press Up/Down in the upgrade bay and confirm selection moves between `Oxygen Tank I` and `Pressure Seal I`.
- Attempt to buy `Oxygen Tank I` without enough resources and confirm the upgrade panel gives clear feedback.
- Bank the required resources, buy `Oxygen Tank I` from the upgrade panel after extraction, restart, and confirm max oxygen is 40.
- Confirm `Pressure Seal I` is locked before scanning `Thermal Vent`, available after the discovery plus required resources, and owned after purchase.
- After buying `Pressure Seal I`, return to the pressure-locked wreck and confirm the shimmer route is open and the inside `Wreck Signal Cache` can be scanned.
- Confirm `Signal Lens I` is locked before scanning `Wreck Signal Cache`, available after the discovery plus required resources, and owned after purchase.
- Buy `Cargo Rack I`, restart a dive, and confirm the HUD starts at `Cargo: 0 / 4`.
- After buying `Cargo Rack I`, collect four resources, extract, and confirm all four bank correctly.
- After buying `Cargo Rack I`, fail with carried cargo and confirm all carried cargo is still lost.
- Confirm `Predator Warning I` is locked before scanning `Gulper Eel`, available after the discovery plus required resources, and owned after purchase.
- After buying `Predator Warning I`, approach the `Gulper Eel` route and confirm warning feedback begins farther away while contact remains possible if the warning is ignored.
- Confirm `Decoy Pulse I` is locked before scanning `Gulper Eel`, remains locked until `Predator Warning I` is owned, becomes available with its required resources afterward, and can be spent once per expedition by re-scanning the `Gulper Eel`. Confirm the predator is pulled away briefly, contact remains possible after the window or on mistiming, and pressure locks, extraction, cargo failure, seeded route selection, and `Burst Thruster` are unchanged.
- Scan `Lantern Fry` with F, confirm oxygen decreases, discovery text appears, and `Glow Plankton` pulses.
- Scan `Thermal Vent` with F, confirm oxygen decreases, discovery text appears, and the low-opacity current hint appears without adding a duplicate `Glow Plankton` pickup or reading like a guaranteed safe path. Re-scan it and confirm the repeat text refreshes the warm-current clue while keeping the glow route optional.
- Approach the pressure gate near the research wreck without `Pressure Seal I` and confirm entry is denied safely with clear feedback that says to buy the upgrade at the surface and return.
- Scan the outside `Pressure-Locked Research Wreck` target and confirm the future-cache signal appears without looking like an immediately open path or disrupting extraction/failure flow.
- Scan a resource pickup with F, confirm oxygen decreases, discovery text records depth band/upgrade use/material need, and matching visible deposits highlight for several seconds.
- Start a fresh expedition, scan the first nearby resource, and confirm the status text says to collect it and return to base to bank cargo.
- After buying `Signal Lens I`, re-scan a discovered resource and confirm status text points toward another matching visible uncollected deposit or says the signal is quiet.
- Re-scan an already discovered resource and confirm no oxygen is spent while the matching-deposit highlight refreshes.
- Approach the deep `Gulper Eel`, confirm the warning current/marker and patrol hint communicate danger before contact, then confirm contact causes oxygen loss plus knockback without instant failure.
- Scan the `Gulper Eel`, confirm the discovery text is recorded, repeat scan stays free, and patrol/chase/contact behavior still works unchanged.
- Collect resources, fail by oxygen depletion, restart, and confirm carried resources were lost while banked resources remain.
- Confirm the HUD depth increases while diving and that the base indicator points back toward the surface base.
- Confirm the scene communicates shallow, midwater, and deep areas through color, transition bands, landmarks, and resource placement. Confirm the `Thermal Vent` pocket reads as a warm optional clue route without obscuring Shell Reef banking, pressure-gate feedback, or safe-return language.
- Let oxygen reach zero and confirm the HUD shows a failure result.
- After first dive loop: start a dive, collect or scan something, return to base, and confirm the result is recorded.

## Vertical Slice Playtest Notes

- First-loop validation: the surface base is visible at start, the HUD labels depth and base direction, and the first shallow resource is visible before the player commits to a deeper route.
- Risk validation: a cautious dive can bank shallow/midwater cargo, a deeper dive can pursue `Glow Plankton`, and the `Gulper Eel` route can turn a deep reward attempt into a close oxygen return or failure.
- Upgrade validation: because cargo has three slots and `Oxygen Tank I` costs four total resources, the first upgrade requires at least two successful banking dives; after purchase, the 40-oxygen run gives room for an extra scan, pickup, or predator recovery before returning.
